-First the folder of the project i.e 
bash
``` cd C:\Users\kylia\OneDrive\Bureau\ecommerce> ```

-Then activate the virtual environment in bash
``` env\Scripts\activate.bat```

-To lunch the site run ``` python manage.py runserver```